{
  "schema": 1,
  "name": "airtap",
  "github": "https://github.com/SuperMarioYL/airtap",
  "host": "airtap.lei6393.com",
  "lang": { "primary": "zh", "toggle": true },
  "accent": ["#3fb950", "#1a7f37"],
  "hero": {
    "eyebrow": { "zh": "本地 · 零出口 · 可审计 · 单文件", "en": "On-prem · Zero egress · Auditable · Single file" },
    "headline": { "zh": "一条命令\n在内网 GPU 盒上跑编码 Agent", "en": "Run a coding agent\non your in-network GPU box" },
    "sub": { "zh": "数据不出境的国企研发，第一次像用云端 runner 一样顺手——agent 的 shell 命令出不了这台盒子，每一次外发都留痕可审，你一断开它就停。", "en": "On-prem R&D that finally feels like a cloud runner — the agent's shell commands can't leave the box, every outbound call is logged and audit-ready, and the moment you disconnect it stops." },
    "command": "airtap run \"给 server.go 加 /health 接口\"",
    "cta": { "zh": "查看 GitHub", "en": "See on GitHub" },
    "ctaGithub": { "zh": "源码 / README", "en": "Source / README" }
  },
  "advantages": [
    {
      "title": { "zh": "连 agent 的 shell 命令都出不了这台盒子", "en": "Even the agent's shell commands can't leave the box" },
      "body": { "zh": "agent 跑的每一条 shell 命令，都在一个没有出口的隔离网络里执行——curl、wget、nc 都拨不出去；盒子若没法这样隔离，命令干脆不跑，绝不留后门。", "en": "Every shell command the agent runs executes inside an isolated network with no way out — curl, wget, nc can't dial off the box; if the box can't enforce that isolation, the command simply won't run. No back door left open." },
      "chips": ["› shell 命令隔离运行", "› 隔离失败即拒绝执行"]
    },
    {
      "title": { "zh": "没有审计记录，就不放行任何外发", "en": "No audit record, no egress" },
      "body": { "zh": "每一次放行与拒绝都写进审计日志；万一审计写不下去（磁盘满、日志损坏），那次拨号直接拒绝——你看到的「零出口」才经得起查。", "en": "Every allow and deny is written to the audit log; if a record can't be made (disk full, broken log), that dial is refused outright — so the \"zero egress\" you see can stand up to an audit." },
      "chips": ["› 放行/拒绝都留痕", "› 审计写失败则不放行"]
    },
    {
      "title": { "zh": "你允许的工具，才是真正会跑的工具", "en": "The tools you allow are the tools that run" },
      "body": { "zh": "在 airtap.yaml 里写 tools:[read]，agent 就真的只能读——写、list、bash 一概不跑；模型也只调盒上的国产模型，不经任何云。你写的约束，就是执行的约束。", "en": "Write tools:[read] in airtap.yaml and the agent really can only read — write, list, bash stay off; the model only ever calls the on-box domestic model, no cloud in the path. The constraint you write is the constraint that runs." },
      "chips": ["› 写 read 就只能读", "› 模型只在 127.0.0.1"]
    },
    {
      "title": { "zh": "你走开，它就停；你的 agent，也能进来", "en": "Walk away and it stops; your own agent can come in" },
      "body": { "zh": "笔记本一断开，盒上的 agent 循环立刻取消——你走后不会有僵尸进程空烧 GPU；命令失败，agent 读得到报错自己改，不用你盯着；出口隔离 + 审计 + 工具白名单现在还是一份插件接口，你自带的 agent 循环实现它就能跑进这座盒子，出口仍由盒子说了算。", "en": "The moment your laptop disconnects, the on-box loop cancels — no zombie process burns the GPU after you walk away; when a command fails, the agent reads the error and self-corrects instead of flying blind; the egress isolation + audit + tool allowlist is now a plug-in interface too, so your own agent loop can run inside the box, with egress still decided by the box." },
      "chips": ["› 断开即停，不空烧 GPU", "› 命令失败，agent 读得到报错", "› 插件接口：自带 agent loop"]
    }
  ],
  "scene": { "kind": "hub-nodes", "nodes": ["笔记本", "mTLS 隧道", "信创 GPU 盒", "本地模型"] },
  "cta": {
    "headline": { "zh": "在内网 GPU 盒上开始", "en": "Start on your in-network GPU box" },
    "sub": { "zh": "一条命令上手，零云上出口。", "en": "One command. Zero cloud egress." },
    "button": { "zh": "查看 GitHub", "en": "See on GitHub" }
  },
  "footer": { "tag": { "zh": "MIT · SuperMarioYL", "en": "MIT · SuperMarioYL" } }
}
